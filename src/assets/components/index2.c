#include<stdio.h>{
    int main(){
        int element;
        printf("Enter the number of element for array -->: ");
        scanf("%d",&element);
        for (i=0,i<=index,i++){
              printf("Enter the number of element for array -->: ");
              scanf("%d",&element);
            int arr[i]=element;
        }
    }
}